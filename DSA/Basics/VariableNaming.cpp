#include <iostream>
using namespace std;

int main()
{
    // can satrt with alphabet or _
    //  special character except _ are not allowed
    // blanks, commas not allowed
    // keywords not allowed

    //! @ # $ % ^ & * ( ) { } [ ] - +
    //= / \ | < > , . ; : ' " ~ `
}